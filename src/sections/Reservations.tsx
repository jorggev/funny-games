import ReservedDates from '../components/ReservedDates'

const Reservations = () => {
  return (
    <section id="reservas" className="my-8">
      <ReservedDates />
    </section>
  )
}

export default Reservations